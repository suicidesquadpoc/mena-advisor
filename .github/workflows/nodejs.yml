name: mena-advisorCI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: test
        ports:
            - '8888:3306'
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    strategy:
      matrix:
        node-version: [10.x, 12.x]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Setup environment variables
      run: |
        cp sample.env .env
    - name: Run npm install
      run: |
        npm install
      env:
        CI: true
    - name: Run code check lint
      run: |
        npm run lint
    - name: MYSQL
      run: |
        mysql -h 127.0.0.1 --port 8888 -u root -ppassword -e 'CREATE DATABASE IF NOT EXISTS db;'
        mysql -h 127.0.0.1 --port 8888 -u root -ppassword -e 'USE db;'
        mysql -h 127.0.0.1 --port 8888 -u root -ppassword -e 'CREATE TABLE coordinates (coordinate_id INT NOT NULL AUTO_INCREMENT PRIMARY KEY, longitude DECIMAL(10,6) SIGNED NOT NULL, latitude DECIMAL(10,6) SIGNED NOT NULL, time DATETIME NOT NULL, elevation DECIMAL (7,2) SIGNED, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);'
    - name: Run test
      run: |
        npm test
    - name: Run npm build
      run: |
        npm run build --if-present
      env:
        CI: true
